import { TestBed, inject } from "@angular/core/testing";

import { TrackInformationService } from "./track-information.service";

/*describe("TrackInformationService", () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TrackInformationService]
    });
  });

  it("should be created", inject([TrackInformationService], (service: TrackInformationService) => {
    expect(service).toBeTruthy();
  }));
});*/
